[project]
name = "py-med-management-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "flask>=3.1.3",
    "uvicorn>=0.41.0",
]
